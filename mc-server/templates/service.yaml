apiVersion: v1
kind: Service
metadata:
  name: {{ include "mc-server.fullname" . }}
  labels:
    {{- include "mc-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mc-server.selectorLabels" . | nindent 4 }}
